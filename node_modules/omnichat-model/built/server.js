"use strict";
const Parse = require("parse/node");
const Item_1 = require("./Item");
const Variant_1 = require("./Variant");
console.log('started');
Parse.initialize(process.env.PARSE_APP_ID, process.env.PARSE_JAVASCRIPT_KEY, process.env.PARSE_MASTER_KEY);
Parse.serverURL = process.env.PARSE_SERVER_URL;
const item = new Item_1.Item({
    quantity: 10
});
item.detail = 'Detalhes';
item.price = 10.40;
item.product = 'Produto Teste';
item.productReference = '1234xyz';
item.variant = new Variant_1.Variant();
item.save(null, { useMasterKey: true })
    .then(() => {
    console.log('saved');
    const query = new Parse.Query(Item_1.Item);
    query.equalTo('objectId', item.id);
    return query.first();
})
    .then((result) => {
    const item2 = result;
    console.log(item2.quantity);
}, error => {
    console.error(error);
});
//# sourceMappingURL=server.js.map